// 根项目构建脚本
// 定义所有子项目共用的插件版本
plugins {
    id("com.android.application") version "8.3.0" apply false  // Android Gradle 插件
    id("org.jetbrains.kotlin.android") version "1.9.22" apply false  // Kotlin Android 插件
}
